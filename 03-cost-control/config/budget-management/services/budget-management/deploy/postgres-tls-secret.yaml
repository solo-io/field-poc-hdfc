---
# Example TLS secret for PostgreSQL connections.
# Replace placeholder values with actual certificates.
# Uncomment volume mounts in deployment YAMLs to enable.
apiVersion: v1
kind: Secret
metadata:
  name: quota-management-db-tls
  namespace: agentgateway-system
  labels:
    app: quota-management
    app.kubernetes.io/managed-by: agentgateway-demo
type: Opaque
stringData:
  ca.crt: |
    -----BEGIN CERTIFICATE-----
    # Replace with your CA certificate
    -----END CERTIFICATE-----
